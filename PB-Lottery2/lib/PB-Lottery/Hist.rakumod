unit module PB::Lottery::Hist;

# Build histogram statistics from draw records.

sub build-histogram(@records --> Str) is export {
    # TODO: compute frequencies for white balls (1..69) and powerball (1..26),
    #       and for DP as well; output deterministic text
    ...
}
